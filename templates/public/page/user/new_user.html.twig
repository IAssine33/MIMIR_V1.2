{% extends 'public/basePublic.html.twig' %}

{% block main %}
<main class="bg_main_form">
    <section class="flex">
        <article class="blocArticle_newUser">
            <h1>Créez votre profil </h1>
            <div class="formWrapper_newUser">
                {{
                form_start(userForm)
                }}
                <div class="div_formUser">
                    <div class="div_firstname">
                        {{ form_row(userForm.firstname) }}
                    </div>
                    <div class="div_lastname">
                        {{ form_row(userForm.lastname) }}
                    </div>
                    <div class="div_email">
                        {{ form_row(userForm.email) }}
                    </div>
                    <div class="div_password">
                        {{ form_row(userForm.password) }}
                    </div>
                        {{
                        form_end(userForm)
                        }}
                </div>
            </div>
        </article>
    </section>
</main>
{% endblock %}