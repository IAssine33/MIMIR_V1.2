{% extends 'public/basePublic.html.twig' %}

{% block main %}
<main>
    <h1> Profil : </h1>

    <div class="formWrapper">
        {{ form_start(userParentForm) }}
        <div class="row">
            <div class="col-2">
                {{ form_row(userParentForm.civility) }}
            </div>
            <div class="col-2">
                {{ form_row(userParentForm.phone) }}
            </div>
            <div class="col-2">
                {{ form_row(userParentForm.adress) }}
            </div>
            <div class="col-2">
                {{ form_row(userParentForm.submit) }}
            </div>
        </div>
        {{    form_end(userParentForm)    }}
    </div>
</main>
{% endblock %}